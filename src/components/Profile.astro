---
import { getStaticFilePath } from '../lib/blog-helpers.ts'
import '../styles/font.css'
import SNSLink from './SNSLink.astro'

// const iconImage = new URL(getStaticFilePath('/icon_hiyoko.png'), Astro.site)

export interface Props {
  isLargeSize: boolean
}

const { isLargeSize } = Astro.props
---

<section class="profile-section">
  <h2>プロフィール</h2>
  <div class={isLargeSize ? '' : 'flex-box'}>
      <div class={`icon-circle ${isLargeSize ? 'large-icon' : 'small-icon'}`}>
          <img src="/icon_hiyoko.png" alt="icon">
      </div>
      <div class="profile-content">
        <p>ま〜ち │ Webエンジニア</p>
        <p class="subtext">初心者に寄り添った説明を心がけています✨</p>
        <p class="friend">＼エンジニアのお友達を募集中😊／</p>
        <SNSLink />
      </div>
  </div>
</section>

{isLargeSize &&(
    
    <section class="profile-section">
      <h2>このサイトで発信していること</h2>
      <div class="site-focus">
        <p>Web系 │ AI関連 │ 作業効率が上がる情報</p>
      </div>
    </section>
)}

<style>
.profile-section {
  color: #191919;
  background-color: #FFF;
}

.profile-section .flex-box{
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.profile-content, .site-focus {
    padding-left: 1rem;
}

.profile-content p, .site-focus p {
  margin: 0;
  color: #3F3F3F;
}

.profile-content p.subtext{
  font-size: 0.8rem;
}
.profile-content p.friend{
  margin-top: 30px;
  text-align: center;
}

.icon-circle{
  border-radius: 50%;
  background-color: #dee1c5;
  margin: 0 auto;
}

.icon-circle img{
  width: 100%;
  height: 100%;
  object-fit: cover;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  margin: auto;
}

.large-icon{
  width: 50%;
  height: 50%;
  margin-bottom: 30px;
}
.small-icon{
  width: 30%;
  height: 30%;
}

</style>